\usepackage[backend=biber,style=alphabetic,sorting=nty,maxalphanames=1]{biblatex}
\renewcommand*{\labelalphaothers}{}
\addbibresource{biblio.bib}
\DeclareNolabel{ % For ignoring spaces in citations
  \nolabel{\regexp{[\p{Z}\p{P}\p{S}\p{C}]+}}
}
\usepackage[english]{babel}
\usepackage{indentfirst} % For indenting the first paragraph
\usepackage{graphicx}
\usepackage{url}
\usepackage{csquotes}
\usepackage{amssymb}
\usepackage{pdfpages}

\usepackage[includeheadfoot, margin=0.7in]{geometry} % For margins, probably buggy
\usepackage[width = 0.75\textwidth, font = small]{caption} % For caption margins

% New style here ------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\scshape\nouppercase{\leftmark}} 
\fancyhead[R]{\scshape\thepage}


% Format code cells ------

\usepackage{listings} % For code formatting

\lstset{frame=leftline,
  language=Python,
  aboveskip=0.2in,
  belowskip=0.2in,
  showstringspaces=false,
  columns=flexible,
  basicstyle={\small\ttfamily},
  numbers=none,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{blue},
  commentstyle=\color{dkgreen},
  stringstyle=\color{mauve},
  breaklines=true,
  breakatwhitespace=true,
  xleftmargin = 0.1\textwidth,
  linewidth=0.8\textwidth,
  numbers=left,
  tabsize=3
} % use in a lstlisting environment

% -------------------

\setcounter{tocdepth}{1} % Set table of content level

\usepackage{setspace} % For spacing
\onehalfspacing

\newcommand{\tsp}{\textsuperscript} %Superscript
\newcommand{\tsb}{\textsubscript} %Superscript
\newcommand{\ie}{\emph{i.e. }}
\newcommand{\eg}{\emph{e.g. }}

% ---- Title and author, provisory
\title{Neural Engineering: Lecture Notes}
\author{Simone Ranaldi}